# Day 3: Python Foundations

## Topics Covered

- Functions:
    -Parameters 
    -Arguments
        -Default Arguments 
        -Arbitary Arguments (*args)
        -Keyword Arbitary Arguments (*kwargs)\
    -Scope
    -Lambda Functions
    -Built-inFunctions():
        -map()
        -filter()
        -zip()
        -enumerate()
    -Modules


## Exercises

1. Simple Interest Calculator
2. Class Average
3. Number Analyzer
4. Shared Booking Counter
5. Temperature  Report Module

## How to Run

Run each file using:

```bash
python exercise-01-simple-interest.py
python exercise-02-class-average.py
python exercise-03-number-analyze.py
python exercise-04-shared-booking-counter.py
python exercise-05-temperature-report-module.py
```

## What I Learned

I learned how to write advanced functions using default settings, extra arguments like *args and *kwargs, and how to change global variables using the global keyword. I also practiced using short lambda functions and built-in tools like map(), filter(), zip(), and enumerate(). Finally, I figured out how to build custom modules and organize my code across different files.

## Challenges Faced

Working with custom modules and importing files caused some tricky errors with relative paths and folder setups. Getting everything to connect properly without breaking required me to be very careful with how I structured my project folders and import statements.